spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/homevault
    username: homevault
    password: homevaultpw
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
    database-platform: org.hibernate.dialect.PostgreSQLDialect
